{
  "name": "globo-systems",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "hello": "echo 'Hello, NPM Scripts!'",
    "dev:sass": "sass --watch src/styles.scss:./dist/styles.css",
    "build:sass": "sass ./src/styles.scss ./dist/styles.css",
    "start": "node src/server.js",
    "dev": "node --watch --env-file=.env src/server.js",
    "prepare": "husky",
    "lint": "eslint . --ext .js,.ts",
    "format": "prettier --write .",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "prebuild": "echo Starting build...",
    "build": "cross-env NODE_ENV=production npm run build:sass && npm run build:js",
    "build:js": "esbuild src/server.js --bundle --platform=node --format=esm --outdir=dist",
    "postbuild": "echo Build complete!",
    "docker:build": "podman build -t globo-systems .",
    "docker:run": "podman run -p 3000:3000 globo-systems",
    "compose:up": "podman compose up",
    "compose:down": "podman compose down"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@fastify/multipart": "^10.1.1",
    "chokidar": "^5.0.0",
    "fastify": "^5.12.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^5.0.1",
    "c8": "^12.0.0",
    "concurrently": "^10.0.5",
    "cross-env": "^10.1.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "prettier": "^3.9.7",
    "sass": "^1.104.1",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.1"
  }
}